---
description: 重定向入口——本项目唯一权威规则在根目录 CLAUDE.md
alwaysApply: true
---

# Cursor 入口

**本项目的唯一权威规则源是根目录的 `CLAUDE.md`。任何代码改动前必须先完整阅读它。**

`CLAUDE.md` 包含：项目定位、硬约束（安全红线、架构分层依赖方向、IPC 三处同步、质量底线、Git 提交推送门控）、规则冲突优先级、施工合同规范（`docs/requirements/`）、场景规则索引、前端验收、收尾沉淀。

当 `CLAUDE.md` 的「agent-skills」场景索引把任务指向某个技能文件时：动手前先读该文件。技能库在仓库根目录 `agent-skills/`。

> 本项目原先的 `.cursor/rules/` 和 `.cursor/skills/` 规则已迁移并归档至 `_archive/cursor-legacy/`，仅作历史参考，不再作为规则来源。所有规则以 `CLAUDE.md` 为准。
