---
description: 开发流程门控，覆盖需求到收尾的完整周期
alwaysApply: true
---

# 开发流程

## Phase 1：接需求

### 逃生口（小改动快速通道）
以下情况可跳过五步流程，简要说明后直接改：
- 单行 typo / 格式修复 / 注释修正
- 单文件 <10 行的小调整，且用户意图明确
- 用户明确说"直接改""帮我改一下"等直接指令

> 不确定是否属于"小改动"时，按新需求流程走。

### 新需求（首次提出）
收到**新需求**时，严格按顺序执行：
1. **思考** — 理解需求的本质目标
2. **提问** — 有不清楚的地方立即问
3. **复述** — 用自己的话复述需求，确认理解
4. **方案** — 列出实现计划
5. **等许可** — 用户确认后才动手编码

### 已批准方案的子任务
用户已审批整体方案后，执行子任务时可简化为：
1. **简要汇报** — 当前要做什么（一句话）
2. **直接执行** — 无需重新走五步流程

✅ 需求变更时先更新方案再编码
✅ 批量修改（>3 文件）先列计划等确认
✅ 新增功能前搜索项目内已有实现，禁止内部重复造轮子

❌ 禁止不经确认直接写代码（指新需求，不是已批准的子任务或逃生口场景）

## Phase 2：研究调研

<HARD-GATE>
接到需求后第一步：**先查项目参考文档，再搜外部资源**，不要直接自己实现。

搜索路径（按优先级）：
1. **Claude Code 源码**（`_reference/framework-harness/repos/claude-code-sourcemap-main/`）— 看看同类产品怎么实现的
2. **Alice 方法论**（`_reference/framework-harness/repos/alice-methodology/`）— 人格化 AI 的设计参考
3. **Alice 编码规范 + Debug 模式分析**（`_reference/alice-*.md`）— UI/UX 和工程实践参考
4. GitHub 仓库/代码 → npm 包 → 社区方案 → 最后才考虑自己写

> ⚠️ **注意**：Cursor 的 Glob/Grep 工具可能因 gitignore 搜不到 `_reference/` 目录。如果搜索返回 0 结果，改用 Shell 工具的 `Get-ChildItem`（Windows）或 `ls`（Mac/Linux）直接访问。用 `Read` 工具读取具体文件不受影响。

✅ Claude Code 源码是 Agent 框架层的第一参考源（1884 个 TS 文件，覆盖工具/上下文/协调/记忆等）
✅ Alice 方法论是人格化/交互设计的第一参考源
✅ 找到 80%+ 匹配的开源项目时，优先 fork/适配/包装
✅ 自己实现前必须说明：搜了什么、为什么现有方案不适用

**最低调研标准**（每个功能模块至少完成以下之一）：
1. 搜索 Claude Code 源码中对应模块的实现并参考
2. 搜索 Alice 方法论/编码规范中的相关章节
3. 搜索 GitHub/npm 是否有可复用的开源实现

**豁免情形**（以下情况可跳过调研，直接说明理由即可）：
- 使用行业标准库/API 的常规集成（如 electron-updater、Web Speech API、cron 解析）
- 纯 UI 调整或 <3 个文件的小改动
- 已批准方案中明确指定了实现方式的子任务

> 豁免时仍需一句话说明："使用 XXX 标准方案，跳过调研"
</HARD-GATE>

## Phase 3：方案设计

- 复杂功能（>3 文件）先写技术方案，列出模块拆分和关键接口
- 涉及架构变更时先更新 `docs/architecture.md`，再动代码
- 方案需要用户确认后才开始编码

## Phase 5：测试

✅ 新功能必须附带测试
✅ 修 Bug 时先写复现测试，再修复

### E2E 测试规范

<HARD-GATE>
E2E 测试必须覆盖**真实对话流程**，不能只检查静态 UI 元素：

1. **真实对话测试** — 通过 UI 发送提示词，等待流式响应，验证回复出现
2. **工具调用测试** — 触发工具调用（如 get_current_time），验证工具执行和结果显示
3. **会话管理测试** — 创建/切换/删除会话，验证消息持久化
4. **设置生效测试** — 修改设置后验证行为变化

技术要求：
- 使用 Playwright + webServer 自动启动 dev server
- API Key 从环境变量 `TEST_LLM_API_KEY` 读取（不硬编码）
- 设置页注入 API Key 和 Base URL 后再跑对话测试
- 流式响应等待用 `page.waitForSelector` / `page.waitForFunction`，超时设 30s
- 每次新增核心功能后补充对应 E2E 测试

❌ 禁止 E2E 只测"元素存在"而不测"功能可用"
</HARD-GATE>

## Phase 6：自审

<HARD-GATE>
编码完成后、提交前，逐项检查以下清单：

**代码质量**
- [ ] 函数不超过 ~50 行，文件不超过 ~800 行
- [ ] 没有重复代码（DRY）/ 过度抽象（YAGNI）
- [ ] 命名清晰，看名字就知道干什么

**错误处理 & 安全**
- [ ] 没有空 catch / 静默吞错
- [ ] 异步操作有超时处理
- [ ] 没有硬编码密钥，错误信息不泄露内部路径

**清理**
- [ ] 没有 console.log 调试残留 / 注释掉的代码块
- [ ] 没有未处理的 TODO/FIXME

**架构**
- [ ] 新代码位置合理，没有跨层调用
- [ ] 新功能有对应测试

发现问题就地修复。

❌ 禁止跳过自审直接进入验证阶段
</HARD-GATE>

## Phase 7：完成验证

<HARD-GATE>
声称任务完成前，必须**按顺序**执行：

1. **自审确认**（不可跳过）— 回顾 Phase 6 清单，逐项在心中确认。如果因为快速迭代跳过了 Phase 6，**必须在此补做**，不能直接跳到第 2 步
2. 运行测试并展示通过结果
3. 确认 build 通过
4. 确认无新增 linter 报错

> ⚠️ 即使用户持续说"继续"，也不能跳过第 1 步。节奏快不是跳过自审的理由。

❌ 禁止未经验证就说"已修复""已完成"
</HARD-GATE>

## Phase 8：Bug 修复红线

调试步骤（按顺序执行）：
1. **复现** — 找到最短复现路径
2. **加 Log** — 在关键节点加日志，缩小范围（格式：`[模块名] 操作描述 + 输入/输出/耗时`）
3. **读代码** — 理解完整业务链路，不要只看报错行
4. **定位根因** — 区分"症状"和"病因"
5. **写复现测试** — 先写一个会失败的测试
6. **最小修复** — 只修根因，不做附带重构
7. **验证** — 测试通过 + 无新增 linter 报错

常见陷阱：异步竞态 / tool_call 配对丢失 / 上下文溢出 / IPC 事件丢失

✅ 每次变更后声明影响范围

❌ 禁止猜测性修复——先定位原因再修

## Phase 9：提交推送

<HARD-GATE>
完成验证（Phase 7）+ 文档更新（Phase 11）后，必须：
1. `git add` 相关文件（不要 add 整个目录，避免误提交 .env 等敏感文件）
2. `git commit` — 遵循 `.cursor/skills/git-workflow.md` 中的 Commit 规范
3. `git push` — 推送到远程（注意代理配置，见 `.cursor/skills/git-workflow.md`）
4. 推送失败时排查代理端口，不要反复盲试

❌ 禁止只改代码不提交
❌ 禁止只提交不推送（除非用户明确说"先不推"）
</HARD-GATE>

## Phase 11：收尾沉淀

<HARD-GATE>
每次对话结束前，更新文档：

**必更新**（每次对话都要）：
1. `docs/progress.md` — 当前状态和时间线
2. `docs/changelog.md` — 本次代码变更记录
3. `docs/architecture.md` — 如果改了架构/新增模块

**按需更新**（有对应变更时才需要）：
- `docs/features.md` — 完成功能时
- `docs/decisions.md` — 做技术决策时
- `docs/api-contracts.md` — 改接口/新增 IPC 时
- `docs/glossary.md` — 引入新概念时
- `docs/pitfalls.md` — 修 Bug 踩坑时

❌ 禁止只更新 progress.md 就结束
</HARD-GATE>

### 规则自进化

- 本次开发中发现规则不合理/缺失/冲突 → 记录到 `docs/rules-feedback.md`
- 累计 3 条以上反馈时，主动建议用户修订规则
- 规则修订后更新版本号和 changelog
