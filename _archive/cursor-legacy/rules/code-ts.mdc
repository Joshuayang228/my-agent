---
description: TypeScript 代码规范，编辑 .ts/.tsx 文件时自动加载
globs: ["**/*.ts", "**/*.tsx"]
---

# TypeScript 代码规范

## 技术栈

- **外壳**：Electron（主进程 Node.js + 渲染进程 Chromium）
- **语言**：TypeScript 全栈，主进程与渲染进程共享类型定义
- **存储**：SQLite（结构化）+ 向量数据库（语义检索）+ 本地文件系统（项目记忆/用户画像）
- **Agent 事件流**：AsyncGenerator 模式

## 编码原则

- **KISS** — 优先最简方案，避免过度抽象
- **DRY** — 重复逻辑提取为共享函数
- **YAGNI** — 不提前建不需要的抽象
- **不可变优先** — 修改数据时创建新对象，不原地修改
- 文件大小上限 ~800 行，超过则拆分
- 函数大小上限 ~50 行，超过则拆分

## 注释规范

✅ 函数注释写三个要素：**背景**（为什么有这个函数）、**意图**（做什么）、**约束**（特殊限制）
✅ 改函数时不准删注释，只能更新

❌ 禁止只写功能描述的注释（"获取用户列表"）
❌ 禁止写显而易见的注释（"i 加 1"）

## 错误处理

✅ 所有 catch 块必须有实际处理逻辑（日志/上报/降级）
✅ 对外接口返回用户友好的错误信息

❌ 禁止空 catch 块
❌ 禁止静默吞错

## 测试

✅ 新功能必须附带测试
✅ 测试框架：vitest 或 jest
✅ 测试目录：`__tests__/` 或同级 `*.test.ts`
✅ 测试文件命名：`{模块名}.test.ts`

## 架构约束（参考 Alice 方法论）

- **工具与服务分离**：AI 可见的是"工具"，AI 不可见的是"服务"
- **事件流边界**：核心逻辑只输出纯数据事件（AgentStreamEvent），UI 层消费事件并渲染
- **声明式工具接口**：每个工具带元数据（isReadOnly / isDestructive / isConcurrencySafe）
- **先读后写**：修改文件前必须先读取过该文件（防幻觉写入）
